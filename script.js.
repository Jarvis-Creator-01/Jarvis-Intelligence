const statusText = document.getElementById('statusText');
const jarvisOrb = document.getElementById('jarvisOrb');

// 100x Knowledge Base (Har spelling handle karega) [cite: 2026-03-05]
const jarvisMemory = {
    "mission": "Humara mission aapko 19 se 20 saal ki umar mein billionaire banana hai. [cite: 2026-03-03]",
    "history": "Mere paas 1000 saal purani history aur har desh ke rules ka data hai. [cite: 2026-03-02]",
    "kaise ho": "Main bilkul theek hoon Tabish bhai, aapka 100x engine ready hai.",
    "hello": "Hello Tabish bhai, 100x knowledge engine active hai. Poochiye kya poochna hai?"
};

const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
let recognition;

function initJarvis() {
    if (SpeechRecognition) {
        recognition = new SpeechRecognition();
        recognition.continuous = true;
        recognition.lang = 'hi-IN';

        recognition.onstart = () => {
            statusText.innerText = "LISTENING...";
            statusText.style.color = "#00ff00";
        };

        recognition.onresult = (event) => {
            const transcript = event.results[event.results.length - 1][0].transcript.toLowerCase();
            processInput(transcript);
        };
        recognition.start();
    }
}

function processInput(input) {
    let response = "Main is par research kar raha hoon apne 100x engine se.";
    
    // Spelling check aur Smart Matching [cite: 2026-03-02]
    if (input.includes("miss") || input.includes("mission")) response = jarvisMemory["mission"];
    else if (input.includes("hist") || input.includes("itihas")) response = jarvisMemory["history"];
    else if (input.includes("kaise")) response = jarvisMemory["kaise ho"];
    else if (input.includes("hello") || input.includes("jarvis")) response = jarvisMemory["hello"];

    speakResponse(response);
}

function speakResponse(text) {
    // Alert Box ko humne yahan se hata diya hai taaki flow na ruke
    statusText.innerText = "JARVIS SPEAKING...";
    jarvisOrb.style.boxShadow = "0 0 70px #ff00ff";
    
    const msg = new SpeechSynthesisUtterance(text);
    msg.lang = 'hi-IN'; 
    window.speechSynthesis.speak(msg);

    msg.onend = () => {
        statusText.innerText = "LISTENING...";
        jarvisOrb.style.boxShadow = "0 0 30px #00ffff";
    };
}

function sendMessage() {
    const inputField = document.getElementById('userInput');
    const message = inputField.value.toLowerCase();
    if (message) {
        processInput(message); // Ab ye seedha jawab dega, alert nahi dikhayega
        inputField.value = '';
    }
}
